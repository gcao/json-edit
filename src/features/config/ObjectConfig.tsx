import React from 'react';
import { ConfigItem } from 'src/jconfig';

export default function ObjectConfig({ config }: { config: ConfigItem }) {
  return (
    <div>
      <div>Object</div>
    </div>
  );
}
